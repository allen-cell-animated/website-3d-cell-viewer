export const enum ViewMode {
  threeD = "3D",
  xy = "XY",
  xz = "XZ",
  yz = "YZ",
}

export const enum RenderMode {
  volumetric,
  maxProject,
  pathTrace,
}

export const enum ImageType {
  segmentedCell,
  fullField,
}
